"""
Strategy layer — sits between signal ingestion and order placement.

Provides backtestable entry filters, exit rules, and position adjustments
that operate on Research signals without requiring LLM calls.
"""
